# Requirements for signin.py
simple-salesforce
python-dotenv
PyJWT
requests

# Workshop calendar sync (ICS feed parsing)
icalendar
recurring-ical-events
# Provides the IANA tz database for zoneinfo on platforms without system tzdata
tzdata

# mfrc522 is Raspberry-Pi specific; install only on Linux
mfrc522; sys_platform == "linux"

# Hardware feedback dependencies (Raspberry Pi specific)
gpiozero; sys_platform == "linux"
adafruit-circuitpython-ssd1306; sys_platform == "linux"
pillow; sys_platform == "linux"
